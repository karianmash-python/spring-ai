package com.ai.springai.config;

import org.springframework.context.annotation.Configuration;

@Configuration
//@EnableWebSecurity
public class SecurityConfig {

//    @Bean
//    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {
//        http
//            .csrf(csrf -> csrf.disable()) // Disabling CSRF for simplicity, consider more granular configuration for production
//            .authorizeHttpRequests(auth -> auth
//                .requestMatchers("/api/v1/etl/**").permitAll() // Allow public access to the ETL endpoint
//                .anyRequest().authenticated() // Secure all other endpoints
//            );
//        return http.build();
//    }

}
